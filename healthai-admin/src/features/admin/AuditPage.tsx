import { Typography, Box } from '@mui/material';

export default function AuditPage() {
    return (
        <Box>
            <Typography variant="h4">Audit Logs</Typography>
            <Typography color="text.secondary" sx={{ mt: 1 }}>
                Journal d'audit (à venir Semaine 6)
            </Typography>
        </Box>
    );
}
